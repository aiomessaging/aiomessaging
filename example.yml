# aiomessaging example configuration file
app:
  debug: True
# queue backend configuration
queue:
  backend: rabbitmq
  virtual_host: /
# key-value storage configuration
kvstore:
  backend: dummy
events:
  example_event:
    event_pipeline:
      - tests.tmp.ExampleFilter
      - tests.tmp.ExampleFilter:
        example_kwarg: 1
    generators:
      - tests.tmp.OneMessage
    message_pipeline:
      - tests.tmp.ExampleFilter
    output:
      - tests.tmp.DeliveryBackend
      - tests.tmp.DeliveryBackend2
